/**
 * Manifest 模块
 *
 * @description Vite 构建 Manifest 加载，用于生产环境 SSR 资源注入
 */

export { ManifestLoader } from './ManifestLoader';
export type { ParsedManifest, ManifestConfig, AssetInfo } from './types';
